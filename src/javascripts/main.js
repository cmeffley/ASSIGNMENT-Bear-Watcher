import { bearForm, buttonActions } from './bearForm';
import buttonActions2 from './river';

import '../styles/main.scss';

const init = () => {
  bearForm();
  buttonActions();
  buttonActions2();
};

init();
